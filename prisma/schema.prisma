
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model fonlar {
  id Int @id @default(autoincrement())
  kod String?
  unvan String?
  tur String?
  getiri1a Decimal?
  getiri3a Decimal?
  getiri6a Decimal?
  getiri1y Decimal?
  getiriyb Decimal?
  getiri3y Decimal?
  getiri5y Decimal?
}